{
  "permissions": {
    "allow": [
      "Bash(cmd /c \"rmdir /s /q dist 2>nul\")",
      "Bash(cmd /c \"rd /s /q dist 2>nul && echo Dist removed || echo Failed to remove dist\")",
      "Bash(powershell -Command \"Remove-Item -Path 'dist' -Recurse -Force -ErrorAction SilentlyContinue\")",
      "Bash(cmd /c \"rmdir /s /q dist 2>nul && echo dist removed || echo failed to remove dist\")"
    ]
  }
}
